<!DOCTYPE html>
<html>
  <head>
    <title>Dog Walks - Bienvenue</title>
    <style>
      body {
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background-color: #ffffff;
      }
      .splash-container {
        text-align: center;
        opacity: 0;
        animation: fadeIn 1s ease-in forwards;
      }
      .splash-image {
        max-width: 80%;
        height: auto;
        margin: 0 auto;
      }
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }
      @keyframes fadeOut {
        from {
          opacity: 1;
          transform: scale(1);
        }
        to {
          opacity: 0;
          transform: scale(1.05);
        }
      }
      .fade-out {
        animation: fadeOut 1s ease-out forwards;
      }
    </style>
  </head>
  <body class="mesh-gradient-custom overflow-hidden">
    <div class="splash-container">
      <%= image_tag "splash.png", class: "splash-image", alt: "Dog Walks Logo" %>
    </div>


    <script>
      setTimeout(function() {
        const container = document.querySelector('.splash-container');
        container.classList.add('fade-out');

        // Attendre que l'animation de disparition soit terminée avant de rediriger
        setTimeout(function() {
          window.location.href = '<%= new_user_session_path %>';
        }, 1000); // 1 seconde pour l'animation de disparition
      }, 4000); // 4 secondes d'affichage avant de commencer la disparition
    </script>

  </body>
</html> 